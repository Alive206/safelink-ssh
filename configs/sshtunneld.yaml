# Auto-generated by sshtunneld on first run.
# Open the web UI to add tunnels — you should not need to edit this file again.

log_level: info
known_hosts: ${USERPROFILE}\.ssh\known_hosts

defaults:
  keepalive_interval: 30s
  keepalive_max_fails: 3
  dial_timeout: 10s
  reconnect_initial: 1s
  reconnect_max: 60s

web:
  addr: 127.0.0.1:8080
  auth:
    # Optional bearer token for scripts/CI.  Leave empty to disable.
    api_token: ""
    users:
      - username: admin
        password_hash: "$2a$12$9ASz1.akfGt5xjoKHy0LX.g7NOXFmAEbNusvZ/q8NQgsrujo8fXOG"
